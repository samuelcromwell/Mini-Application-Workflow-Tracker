# Copy to .env.local for local overrides (optional).
# On Vercel: Project → Settings → Environment Variables

# Production API (Render) — include /api suffix
VITE_API_BASE_URL=https://your-service-name.onrender.com/api
